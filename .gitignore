node_modules/
.wrangler/
.shots-tmp/
.dev.vars
.dev.local
dist/
*.log
.DS_Store

# Agent worktrees are checked out inside the repo; they are not source.
.claude/worktrees/

# Every variant, not just the exact name — a .bak of a secrets file is still a secrets file.
.dev.vars*
